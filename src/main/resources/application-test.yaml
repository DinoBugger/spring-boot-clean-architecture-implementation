spring:
  docker:
    compose:
      file: docker-compose-test.yaml
      skip:
        in-tests: false
      lifecycle-management: start_and_stop    #This will start the test database before tests and stop it afterward